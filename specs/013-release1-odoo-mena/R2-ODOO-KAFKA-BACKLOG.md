# R2 Backlog — Full Odoo Event-Driven Integration (Deferred from External Proposal)

**Status**: Deferred — not in Release 1 scope (Constitution VII, spec 013 §3.3)  
**Reference**: External Odoo 19 impact map (Kafka + 51 files)  
**R1 delivered path**: Connector XML-RPC batch sync → shared CDM → fea-svc → Control Tower

---

## Do not build for R1

| Item | R2 phase | Notes |
|------|----------|-------|
| `odoo_sync_orchestrator.py` + Kafka publish | R2+ | R1 uses `sync_engine.py` |
| 10× `ipe.odoo.*` Kafka topics | R2+ | Batch sync sufficient |
| Per-service `odoo_*_consumer.py` (6 services) | R2+ | CDM hub pattern in R1 |
| 10 service-level Alembic migrations (`source`/`external_id`) | R2+ | CDM uses `erp_source_id` |
| Integration Hub UI (`/integrations/odoo`) | R1.1 thin | Admin Odoo tab delivered T049 |
| Source badges on 15 planning pages | R2 | |
| Kong rate limit + circuit breaker on `/erp/odoo/*` | R1.1 | |
| Odoo service in IPE docker-compose | Never | Customer/host managed |

## R1.1 candidates (post-UAT)

- Thin Integration Hub page (sync status + trigger over existing APIs)
- Vault-encrypted Odoo credentials (T081)
- `stock.quant` → dedicated inventory table (beyond safety_stock proxy)
- CI release1 compose gate hardened with seed + migrations

## R2 candidates (customer #2+)

- Kafka fan-out if real-time multi-service ingestion required
- SAP B1 / D365 live connectors
- Variance report (IPE recs vs Odoo POs)
- Scenario import from Odoo snapshot

---

*Generated by speckit implement T097 — 2026-06-24.*
