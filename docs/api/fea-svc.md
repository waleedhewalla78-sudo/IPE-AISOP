# fea-svc — Feasibility Scorer

## Endpoints

- `GET /api/v1/health` — Health check
- `GET /api/v1/ready` — Readiness (with DB check)
- `POST /api/v1/feasibility/score` — Composite feasibility scoring (Phase 1)
