# cap-svc — Capacity & Resource Analyzer

## Endpoints

- `GET /api/v1/health` — Health check
- `GET /api/v1/ready` — Readiness (with DB check)
- `POST /api/v1/capacity/schedule` — Finite capacity schedule (Phase 1)
