# Optional custom domain vhost for Star Trans (BATCH2-1)
# Lab web-ui already listens on :8082. Do not enable this on the shared lab.

# server {
#     listen 443 ssl;
#     server_name ipe.startrans.eg;
#     # ssl_certificate     <SECRET_PLACEHOLDER>;
#     # ssl_certificate_key <SECRET_PLACEHOLDER>;
#     location / {
#         proxy_pass http://web-ui:80;
#     }
# }
