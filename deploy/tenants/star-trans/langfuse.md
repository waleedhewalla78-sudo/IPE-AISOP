# Langfuse — Star Trans project (BATCH2-1)

Project name: `ipe-star-trans`
Filter: `tenant_id` = lab `a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11` until production uuid5 is cut over.

Keys live in secrets manager (`LANGFUSE_*` in `env.production`). **Not created** in this prompt — no Langfuse API call without credentials.
