apiVersion: apps/v1
kind: Deployment
metadata:
  name: ipe-res-svc
  namespace: ipe
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ipe-res-svc
  template:
    metadata:
      labels:
        app: ipe-res-svc
    spec:
      containers:
        - name: res-svc
          image: ghcr.io/ipe/res-svc:latest
          ports:
            - containerPort: 8005
          envFrom:
            - configMapRef:
                name: ipe-config
            - secretRef:
                name: ipe-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8005
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/v1/ready
              port: 8005
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: ipe-res-svc
  namespace: ipe
spec:
  selector:
    app: ipe-res-svc
  ports:
    - port: 8005
      targetPort: 8005
