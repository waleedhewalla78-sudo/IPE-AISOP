apiVersion: apps/v1
kind: Deployment
metadata:
  name: ipe-nlp-svc
  namespace: ipe
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ipe-nlp-svc
  template:
    metadata:
      labels:
        app: ipe-nlp-svc
    spec:
      containers:
        - name: nlp-svc
          image: ghcr.io/ipe/nlp-svc:latest
          ports:
            - containerPort: 8007
          envFrom:
            - configMapRef:
                name: ipe-config
            - secretRef:
                name: ipe-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8007
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/v1/ready
              port: 8007
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: ipe-nlp-svc
  namespace: ipe
spec:
  selector:
    app: ipe-nlp-svc
  ports:
    - port: 8007
      targetPort: 8007
