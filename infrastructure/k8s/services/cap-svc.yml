apiVersion: apps/v1
kind: Deployment
metadata:
  name: ipe-cap-svc
  namespace: ipe
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ipe-cap-svc
  template:
    metadata:
      labels:
        app: ipe-cap-svc
    spec:
      containers:
        - name: cap-svc
          image: ghcr.io/ipe/cap-svc:latest
          ports:
            - containerPort: 8003
          envFrom:
            - configMapRef:
                name: ipe-config
            - secretRef:
                name: ipe-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/v1/ready
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "2"
              memory: "2Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: ipe-cap-svc
  namespace: ipe
spec:
  selector:
    app: ipe-cap-svc
  ports:
    - port: 8003
      targetPort: 8003
