apiVersion: apps/v1
kind: Deployment
metadata:
  name: ipe-fea-svc
  namespace: ipe
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ipe-fea-svc
  template:
    metadata:
      labels:
        app: ipe-fea-svc
    spec:
      containers:
        - name: fea-svc
          image: ghcr.io/ipe/fea-svc:latest
          ports:
            - containerPort: 8004
          envFrom:
            - configMapRef:
                name: ipe-config
            - secretRef:
                name: ipe-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8004
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/v1/ready
              port: 8004
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: ipe-fea-svc
  namespace: ipe
spec:
  selector:
    app: ipe-fea-svc
  ports:
    - port: 8004
      targetPort: 8004
