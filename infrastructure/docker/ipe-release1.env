# Release 1 profile overrides (Constitution VII — Kafka/Vault optional)
IPE_KAFKA_BOOTSTRAP_SERVERS=
IPE_KAFKA_ENABLED=false
VAULT_ENABLED=false
IPE_VAULT_ENABLED=false
SECRETS_PROVIDER=env
IPE_AUDIT_REQUEST_MIDDLEWARE=false
AUTH_MODE=local
AUTH_PROVIDER=local
IPE_JWT_SIGNING_MODE=hs256
