# Logrotate template for file-based IPE logs (if not using Docker json-file driver).
# Install: sudo cp infrastructure/logrotate/ipe.conf /etc/logrotate.d/ipe

/var/log/ipe/*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    create 0640 root adm
}
