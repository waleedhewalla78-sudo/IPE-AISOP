apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: postgres-failover
  namespace: ipe-platform
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - ipe-platform
    labelSelectors:
      app: postgres
  scheduler:
    cron: "@every 12h"
