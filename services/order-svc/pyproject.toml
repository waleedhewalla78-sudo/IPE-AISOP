[project]
name = "ipe-order-svc"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["ipe-shared>=0.1.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.uv.sources]
ipe-shared = { workspace = true }

[dependency-groups]
dev = ["pytest>=9.1.0"]
